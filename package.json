{
  "name": "talyssonoc_blog",
  "version": "2.8.3",
  "private": true,
  "dependencies": {
    "hexo-admin": "^0.2.0",
    "hexo-renderer-ejs": "*",
    "hexo-renderer-marked": "*",
    "hexo-renderer-mathjax": "^0.5.0",
    "hexo-renderer-stylus": "*"
  },
  "scripts": {
    "server": "hexo clean && hexo generate && hexo server",
    "deploy": "hexo clean && hexo generate && hexo deploy"
  }
}
